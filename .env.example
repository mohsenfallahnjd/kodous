# Get this from Neon (https://neon.tech) or add Neon from Vercel Marketplace
# On Vercel, DATABASE_URL is auto-injected when you add the Neon integration
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Auth (required for login)
# Admin credentials (can add/edit/delete team members)
AUTH_USERNAME=admin
AUTH_PASSWORD=your-secure-password

# Optional non-admin account (read-only member management)
USER_USERNAME=member
USER_PASSWORD=your-member-password

# Session secret - must be at least 32 characters
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-32-char-minimum-secret-key-here
